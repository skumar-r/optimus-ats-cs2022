import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.optimus.mobile',
  appName: 'ats-mobile-ui',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
